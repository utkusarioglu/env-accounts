{
  "name": "Existing Docker Compose (Extend)",
  "dockerComposeFile": ["../docker-compose.dev.yml", "docker-compose.yml"],
  "service": "env-accounts",
  "workspaceFolder": "/workspace",
  "settings": {
    "workbench.colorCustomizations": {
      "activityBar.background": "#740"
    }
  },
  "extensions": []
}
